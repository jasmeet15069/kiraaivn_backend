{
  "_comment": "External SYSTEM connectors — NOT the VPS this app runs on. Copy to connectors.json to enable. Execution is currently a stub (see README 'System connector' section) pending an SSH library install that the auto-mode security classifier held back for explicit human approval.",
  "connectors": {
    "example-external-host": {
      "host": "203.0.113.10",
      "port": 22,
      "username": "deploy",
      "key_env": "EXAMPLE_HOST_SSH_KEY_PATH"
    }
  }
}
