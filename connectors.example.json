{
  "_comment": "Names of machines expected to connect via agent_client.py (see agent_gateway.py). Copy to connectors.json to enable. A name here doesn't grant access by itself — the machine still has to actually run agent_client.py with the right AGENT_GATEWAY_TOKEN before Jarvis can reach it.",
  "connectors": {
    "my-laptop": {}
  }
}
